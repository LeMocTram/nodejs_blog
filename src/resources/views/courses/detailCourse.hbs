<div class="mt-4">
    <div class="row">
        <div class="col-lg-4">
            <div class="container-button-study">
                <button>Hoc Ngay</button>
                <p>Thời lượng:</p>
                <p>Trình độ:</p>

            </div>

        </div>
        <div class="col-lg-8">
            <h2>{{course.name}}</h2>
            <iframe width="850" height="422" src="https://www.youtube.com/embed/{{course.videoId}}"
                title="{{course.name}}" frameborder="0"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                allowfullscreen></iframe>
            <p>{{course.description}} </p>
        </div>
    </div>
</div>